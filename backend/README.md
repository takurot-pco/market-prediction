# Market Prediction Backend

FastAPI-based backend for the internal prediction market system.

## Setup

```bash
poetry install
poetry run pytest
```

## Development

```bash
poetry run uvicorn app.main:app --reload
